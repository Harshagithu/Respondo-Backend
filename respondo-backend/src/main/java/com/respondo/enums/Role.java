package com.respondo.enums;

/**
 * The four platform roles. Public registration may only ever produce
 * CITIZEN — DISPATCHER and RESPONDER are granted administratively.
 */
public enum Role {
    ADMIN,
    DISPATCHER,
    RESPONDER,
    CITIZEN
}
